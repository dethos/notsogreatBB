<div class="page-header">
  <h1>
    <%= @forum.title %>
    <small>
      <%= @forum.description %>
    </small>
    <div class="pull-right"><p><%= link_to 'New Topic', new_topic_path(f: @forum), class: "btn btn-default" %></p></div>
  </h1>
</div>

<%= render "topics/topic_list" %>

<p>
  <%= link_to 'Edit', edit_forum_path(@forum) %> |
  <%= link_to 'Back', forums_path %>
</p>
