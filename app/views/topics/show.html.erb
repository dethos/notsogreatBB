<div class="page-header">
  <h1>
    <%= @topic.title %>
    <div class="pull-right"><p><%= link_to 'Reply', new_message_path(t: @topic.id), class: "btn btn-default" %></p></div>
  </h1>
</div>

<%= render "messages/message_list" %>

<%= link_to 'Edit', edit_topic_path(@topic) %> |
<%= link_to 'Back', forum_path(@topic.forum_id) %>
